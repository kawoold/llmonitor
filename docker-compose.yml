version: "3.8"

services:
  llmonitor:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - llmonitor_data:/data
    restart: unless-stopped

volumes:
  llmonitor_data:
